using Gameplay.Particles;
using UnityEngine;

public class ParentController : MonoBehaviour
{
    public void Start()
    {
        //Singleton.instance.particleGod.GenerateParticle(Particle.Type.Parent, transform.position);
    }
}
